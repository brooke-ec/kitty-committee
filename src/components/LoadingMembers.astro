<ol>
	{
		[...Array(5)].map(() => (
			<li>
				<div class="avatar" />
				<div>
					<div class="name" />
					<div class="rank" />
				</div>
			</li>
		))
	}
</ol>

<style lang="scss">
	@mixin shimmer($width: 100px) {
		background: linear-gradient(100deg, #353544 20%, #515166 50%, #353544 80%);
		animation: shimmer-#{$width} linear 1s infinite;
		background-size: $width 100%;

		@keyframes shimmer-#{$width} {
			0% {
				background-position: 0 0;
			}
			100% {
				background-position: $width 0;
			}
		}
	}

	ol {
		list-style: none;
		flex-wrap: wrap;
		display: flex;
		gap: 40px;
	}

	li {
		display: flex;
		gap: 10px;

		a {
			color: inherit;
		}

		.avatar {
			@include shimmer(100px);
			border-radius: 10px;
			height: 100px;
			width: 100px;
		}

		.name {
			@include shimmer(200px);
			border-radius: 10px;
			margin-bottom: 5px;
			width: 200px;
			height: 33px;
		}

		.rank {
			@include shimmer(72px);
			border-radius: 10px;
			height: 20px;
			width: 72px;
		}
	}
</style>
